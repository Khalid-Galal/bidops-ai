# Environment Variables for Render.com Deployment
# Copy these to your Render.com service settings

# ==============================================
# Backend API Environment Variables
# ==============================================

# Database (from Render PostgreSQL service - use Internal URL)
DATABASE_URL=postgresql+asyncpg://[POSTGRES_INTERNAL_URL]

# Redis (from Render Redis service - use Internal URL)
REDIS_URL=redis://[REDIS_INTERNAL_URL]

# Security (generate a random string)
SECRET_KEY=[CLICK_GENERATE_IN_RENDER]

# Environment
ENVIRONMENT=production
DEBUG=false

# Google Gemini API (optional but recommended)
GOOGLE_API_KEY=[YOUR_GOOGLE_API_KEY]

# Server
HOST=0.0.0.0
PORT=8000

# Optional - Leave empty for demo
QDRANT_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=


# ==============================================
# Frontend Static Site Environment Variables
# ==============================================

# API URL (your backend service URL + /api/v1)
VITE_API_URL=https://[YOUR-BACKEND-NAME].onrender.com/api/v1


# ==============================================
# HOW TO GET VALUES:
# ==============================================

# 1. DATABASE_URL:
#    - Go to your PostgreSQL service in Render
#    - Copy "Internal Database URL"
#    - Replace postgresql:// with postgresql+asyncpg://
#
# Example:
# postgresql://user:pass@host/db
# becomes:
# postgresql+asyncpg://user:pass@host/db

# 2. REDIS_URL:
#    - Go to your Redis service in Render
#    - Copy "Internal Redis URL"
#
# Example:
# redis://red-xxxxx:6379

# 3. SECRET_KEY:
#    - Click "Generate" button in Render dashboard
#    - Or generate with: openssl rand -hex 32

# 4. GOOGLE_API_KEY:
#    - Go to https://makersuite.google.com/app/apikey
#    - Create API key
#    - Enable Gemini API

# 5. VITE_API_URL:
#    - After backend is deployed, copy its URL
#    - Add /api/v1 to the end
#
# Example:
# https://bidops-api.onrender.com/api/v1
